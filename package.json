{
  "name": "newman-demo",
  "version": "1.0.0",
  "scripts": {
    "test:api": "newman run postman/demo-collection.json -e postman/demo-environment.json --reporters cli,junit --reporter-junit-export reports/junit.xml"
  },
  "devDependencies": {
    "newman": "^5.0.0"
  }
}
